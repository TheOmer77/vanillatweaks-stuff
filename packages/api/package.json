{
  "name": "api",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "pnpm --silent lint && bun build src/index.ts --target bun --minify --outdir dist",
    "lint": "tsc && eslint --max-warnings=0 src"
  },
  "dependencies": {
    "@hono/node-server": "^1.8.2",
    "@hono/zod-validator": "^0.2.0",
    "core": "workspace:*",
    "hono": "^4.1.3",
    "zod": "^3.22.4"
  }
}
